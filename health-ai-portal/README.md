# Health AI Portal

Персональный веб-портал для clinical decision-support системы.

> **ВАЖНО:** Перед внесением изменений прочитай документацию:
> - [../00_context_history.md](../00_context_history.md) — главный контекст проекта
> - [ARCHITECTURE.md](ARCHITECTURE.md) — архитектура системы
> - [IMPROVEMENTS.md](IMPROVEMENTS.md) — журнал изменений
> - [WORKPLAN.md](WORKPLAN.md) — план работ

---

## Технологии

| Компонент | Стек |
|-----------|------|
| Backend | Go 1.22 + Chi Router + sqlx |
| Frontend | React 18 + Vite + Tailwind CSS |
| Database | PostgreSQL 16 |
| Deploy | Docker Compose |

---

## Быстрый старт

### 1. Настройка окружения

```bash
cd health-ai-portal

# Создать файл переменных окружения
cp .env.example .env

# Отредактировать .env при необходимости
nano .env
```

### 2. Запуск через Docker

```bash
# Запуск всех сервисов
docker-compose up -d

# Или через make
make up
```

**Доступ:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:8080
- PostgreSQL: localhost:5432

### 3. Локальная разработка

```bash
# Установить зависимости
cd backend && go mod download
cd ../frontend && npm install

# Запустить PostgreSQL
docker-compose up -d postgres

# Запустить backend (в одном терминале)
cd backend && go run ./cmd/server

# Запустить frontend (в другом терминале)
cd frontend && npm run dev
```

---

## Структура проекта

```
health-ai-portal/
├── ARCHITECTURE.md     # 📐 Архитектура системы
├── IMPROVEMENTS.md     # 📝 Журнал изменений
├── WORKPLAN.md         # 📋 План работ
├── README.md           # 📖 Этот файл
│
├── backend/            # Go API сервер
│   ├── cmd/server/     # Entry point
│   ├── internal/
│   │   ├── config/     # Конфигурация
│   │   ├── database/   # PostgreSQL + миграции
│   │   ├── handlers/   # HTTP handlers
│   │   ├── models/     # Data models
│   │   ├── services/   # Business logic (TODO)
│   │   ├── ai/         # Claude API (TODO)
│   │   └── middleware/ # CORS, auth
│   └── pkg/            # Shared packages
│
├── frontend/           # React приложение
│   ├── src/
│   │   ├── api/        # API клиент
│   │   ├── components/ # React компоненты
│   │   ├── pages/      # Страницы
│   │   ├── hooks/      # React hooks
│   │   ├── types/      # TypeScript типы
│   │   └── lib/        # Утилиты
│   └── public/
│
├── scripts/
│   └── seed.sql        # Seed данные из протокола
│
├── docker-compose.yml
├── Makefile
└── .env.example
```

---

## API Endpoints

### Health
```
GET /health                      # Проверка состояния
```

### Supplements (Стек препаратов)
```
GET    /api/supplements           # Список (фильтры: status, category)
POST   /api/supplements           # Создать препарат
GET    /api/supplements/:id       # Получить по ID
PUT    /api/supplements/:id       # Обновить
DELETE /api/supplements/:id       # Удалить (soft delete)
GET    /api/supplements/schedule  # Расписание на день
GET    /api/supplements/by-category # Группировка по категориям
```

### Goals (Цели)
```
GET    /api/goals                 # Список (сортировка по приоритету)
POST   /api/goals                 # Создать цель
GET    /api/goals/:id             # Получить по ID
PUT    /api/goals/:id             # Обновить
DELETE /api/goals/:id             # Удалить
```

### Labs (Анализы)
```
GET    /api/labs                  # Список (фильтр: category)
POST   /api/labs                  # Добавить результат
GET    /api/labs/:id              # Получить по ID
PUT    /api/labs/:id              # Обновить
DELETE /api/labs/:id              # Удалить
GET    /api/labs/marker/:name     # История по маркеру
GET    /api/labs/trends           # Тренды всех маркеров
```

---

## Функции портала

| Страница | Путь | Описание |
|----------|------|----------|
| Dashboard | `/` | Обзор целей, расписание, алерты |
| Стек | `/supplements` | Управление 45+ препаратами |
| Анализы | `/labs` | История и тренды показателей |
| Циклы | `/cycles` | Аналитические циклы с AI |
| Тренировки | `/workouts` | План и прогрессия |
| Профиль | `/profile` | Данные пользователя |

---

## Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DB_HOST` | Хост PostgreSQL | localhost |
| `DB_PORT` | Порт PostgreSQL | 5432 |
| `DB_USER` | Пользователь БД | healthai |
| `DB_PASSWORD` | Пароль БД | healthai123 |
| `DB_NAME` | Имя базы данных | healthai |
| `SERVER_PORT` | Порт backend | 8080 |
| `JWT_SECRET` | Секрет для JWT | — |
| `CLAUDE_API_KEY` | API ключ Anthropic | — |

---

## Команды Make

```bash
make up           # Запустить все в Docker
make down         # Остановить контейнеры
make logs         # Показать логи
make clean        # Удалить volumes
make build        # Собрать образы

make backend-dev  # Только backend (dev)
make frontend-dev # Только frontend (dev)
make install      # Установить зависимости

make seed         # Загрузить seed данные
make fmt          # Форматирование кода
```

---

## Данные

Seed данные загружаются из `scripts/seed.sql` и содержат:
- **45+ препаратов** из PROTOCOL_v12.md
- **8 целей** с приоритетами
- **Взаимодействия** препаратов
- **Риски** и мониторинг
- **Пример цикла** CYCLE-0002

---

## Документация

| Файл | Назначение |
|------|------------|
| [ARCHITECTURE.md](ARCHITECTURE.md) | Компоненты, потоки данных, схема БД |
| [IMPROVEMENTS.md](IMPROVEMENTS.md) | Журнал done/doing/todo |
| [WORKPLAN.md](WORKPLAN.md) | Спринты и задачи |
| [../00_context_history.md](../00_context_history.md) | Главный контекст проекта |

---

## Лицензия

Личное использование.
